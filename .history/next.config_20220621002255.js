/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['png2.cleanpng.com', 'images.pexels.com/photos', 'links.papareact.com'],
  },
}
